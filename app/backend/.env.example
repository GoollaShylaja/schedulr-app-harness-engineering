# Copy to .env for local overrides (never commit the real .env file).

DATABASE_URL=jdbc:postgresql://localhost:5435/schedulr
DATABASE_USERNAME=schedulr
DATABASE_PASSWORD=schedulr

# Generate a strong random secret for any non-local profile.
JWT_SECRET=dev-secret-change-me
JWT_EXPIRATION_MINUTES=1440

CORS_ORIGIN=http://localhost:3000
